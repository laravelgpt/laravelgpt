{
  "name": "laravelgpt-infra",
  "version": "1.0.0",
  "description": "Infrastructure for LaravelGPT",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "nuxt typecheck && echo 'running typecheck' && tsc -build && echo 'typecheck done'",
    "build": "nuxt build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint --fix ."
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.0.0",
    "@nuxt/eslint": "^1.4.0",
    "@types/node": "^22.15.12",
    "alchemy": "^0.16.6",
    "eslint": "^9.26.0",
    "nuxt": "^3.17.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1",
    "vue-tsc": "^2.2.10"
  },
  "devDependencies": {
    "miniflare": "^4.20250428.1"
  }
}
